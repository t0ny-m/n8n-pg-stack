# Portainer Configuration
#
# No environment variables required for basic setup.
#
# Access Portainer via:
# - NPM proxy: Configure in Nginx Proxy Manager
# - Cloudflare Tunnel: Configure public hostname
# - Direct: Uncomment ports in docker-compose.yml (9443, 8000)
#
# Default credentials are set on first web access.
